<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
        <% link.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= form.label 'URL to link to' %>
      <%= form.text_field :url, placeholder: 'e.g., http://my-very-long-url.com'  %>
    </div>

  <div class="field">
    <%= form.label 'New short link' %>
    <%= form.text_field :slug, placeholder: 'e.g, /shrt-link' %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
